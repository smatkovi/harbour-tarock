import QtQuick
import QtQuick.Controls

// The button the shared table uses for bids, announcements and the talon;
// the counterpart of sailfish/TableButton.qml.
Button {
    font.pixelSize: Theme.fontSizeSmall
}
