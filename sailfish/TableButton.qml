import QtQuick 2.6
import Sailfish.Silica 1.0

// The button the shared table uses for bids, announcements and the talon.
// Narrow by default so a row of them fits the portrait table.
Button {
    preferredWidth: Theme.buttonWidthExtraSmall
}
